# Saved Machine Learning Models Directory

This directory stores serialized model artifacts (`.joblib` format) generated by `ml/src/train_model.py`.

Note: `.joblib` model binary files are excluded from Git tracking via `.gitignore`.
