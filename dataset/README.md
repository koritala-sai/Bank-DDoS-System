# Network Traffic Datasets

This directory manages datasets for ML model training and evaluation:

- `raw/`: Unprocessed benchmark datasets (e.g. CICDDoS2019, CICIDS2017).
- `processed/`: Preprocessed, cleaned, and feature-selected CSV datasets ready for ML training.

Note: Raw and processed binary dataset files are excluded from Git version control via `.gitignore`.
